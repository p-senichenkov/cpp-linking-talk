# Name mangling example

Example showing how names are mangled with and without `extern "C"` specifier in different cases.

## Running

Compile with
```bash
g++ main.cpp -o main.o
```

Explore names with
```bash
nm main.o
```
